using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace V8Gate {
  public static class V8GateExtensions {
    /// <summary>
    /// Removes and returns the object at the top of the List <typeparamref name="T"/>
    /// </summary>
    /// <typeparam name="T"></typeparam>
    /// <param name="list"></param>
    /// <returns></returns>
    //public static T Pop<T>(this List<T> list) {
    //  int lastIndex = list.Count - 1;
    //  T o = list[lastIndex];
    //  list.RemoveAt(lastIndex);
    //  return o;
    //}
  }
}
